{
  "name": "@loadbearing/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "build": "node -e \"console.log('@loadbearing/shared has no build step - it is consumed as TypeScript source')\""
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
