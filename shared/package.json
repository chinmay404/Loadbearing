{
  "name": "@loadbearing/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "//": "This package used to point main at src/index.ts. Vite and tsx load TypeScript happily, so it worked everywhere it was tested — and then failed on the one host that runs plain Node, which could not import a .ts file. A package that claims to be a package has to ship JavaScript.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch --preserveWatchOutput",
    "prepare": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
